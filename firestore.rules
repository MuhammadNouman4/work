rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Portfolio projects: anyone can view, only a signed-in admin can change them.
    match /projects/{projectId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
